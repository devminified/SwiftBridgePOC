import SwiftUI

struct SwiftView: View {
    var body: some View {
        VStack {
            Text("Swift View Bridge POC")
                .padding()
                .background(Color.blue)
                .foregroundColor(.white)
        }
    }
}
